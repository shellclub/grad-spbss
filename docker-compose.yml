version: '3.8'
services:
  php-apache:
    image: php:8.2-apache
    container_name: grad-spbss-php-apache-1
    restart: always
    ports:
      - "8082:80"
    volumes:
      - ${PWD}:/var/www/html  # ใช้ ${PWD} เพื่อระบุโฟลเดอร์ปัจจุบันที่ Portainer เก็บไฟล์ไว้